import { Character } from "./Character.model";

export class Team {
    constructor(
        id: string,
        name: String,
        description: String,
        heroes: Character,
    ){ }
}
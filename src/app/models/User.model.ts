import { Team } from "./Team.model";

export class User {
    constructor(
        id: string,
        email: string,
        displayName: string,
        emailVerified: boolean,
        photoURL: string,
        team : string
    ){ }
}